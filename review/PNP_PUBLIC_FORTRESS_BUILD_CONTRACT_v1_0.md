# Problem No Problem Public Fortress Build Contract v1.0

Date: `2026-09-13`

Status: `BUILD_CONTRACT / NOT_YET_FORTRESS_CLOSED`

Template authority: `Navier-Stokes ANEA Public Fortress Closeout v1.0`

## Commander's intent

Build `TRYX-Relay/TRYX-PNP-MATHIC` into a self-contained public review fortress using the Navier-Stokes ANEA fortress architecture as the operational template, while preserving the exact Problem No Problem mathematical claim boundary.

A reviewer must be able to enter the PNP repository alone and:

1. identify the canonical MATHIC score and source authority;
2. understand the existential Boolean-fold semantics;
3. reproduce the finite benchmark package;
4. inspect the Lean and independent-checker verification path;
5. see exactly what is proved, replayed, verified, and still unproved;
6. cite the release and provenance without relying on Navier-Stokes or Snowman public repositories.

No other public research release may be required for review, reproduction, citation, audit, or challenge.

## Governing source state

```text
PUBLIC_FORTRESS_TARGET = TRYX-PNP-MATHIC
RESEARCH_NAME = Problem No Problem / P vs NP
CANONICAL_REVIEW_TARGET = Problem_No_Problem_MATHIC
SCORE_AUTHORITY = TRYX.PNP.LOCAL.CLOSURE.MATHIC.SCORE.LYRIC.SUCCESSOR.260905.LOCKED.html
GOVERNING_SOURCE = TRYX.PNP.CONTINUITY.CLOSURE.REINTRODUCTION.BENCHMARK.260831.235122Z.md
ACCEPTED_PNP_COMMIT = 496c55eb628588d1afbb49233ab8b91ddb82a271
FRAMEWORK = TRYX / ENIAD / MATHIC
OTHER_PUBLIC_RELEASE_DEPENDENCY = NONE
CROSS_RELEASE_REVIEW_ROUTING = NONE
```

Historical locked artifacts that use older terminology such as `ENAID` remain byte-preserved. New fortress-facing material uses `ENIAD`.

## Mathematical scope to preserve

The current public package establishes the saved Boolean fold law and semantic correctness of repeated current-state existential elimination over every finite Boolean assignment carrier represented by the formalization.

The fortress must preserve the following distinction:

```text
BOOLEAN_FOLD_IDENTITY = VERIFIED
FINITE_CARRIER_EXISTENTIAL_SEMANTICS = LEAN_VERIFIED
SAT_UNSAT_TERMINAL_SEMANTICS = LEAN_VERIFIED
FINITE_PACKAGE_REPLAY = VERIFIED
BENCHMARK_FORMULAS = 17902
BENCHMARK_FOLDS = 53706
BENCHMARK_DISAGREEMENTS = 0
STANDARD_MACHINE_POLYNOMIAL_RUNTIME_BOUND = OPEN
REPRESENTATION_SIZE_BOUND = OPEN
NORMALIZATION_COST_BOUND = OPEN
WITNESS_RECONSTRUCTION_COMPLEXITY = OPEN
CONVENTIONAL_P_EQUALS_NP = NOT_ESTABLISHED
CLAIM_PROMOTION = NONE
```

The fortress must not rename semantic closure into a conventional P=NP proof.

## Required public review path

The completed fortress shall route reviewers in this order:

```text
README
  -> canonical Problem No Problem MATHIC score
  -> governing source / manuscript
  -> Lean formalization scope
  -> Lean + independent-checker verification receipt
  -> finite replay / benchmark evidence
  -> reviewer manifest
  -> parked-release / fortress-closeout record
  -> citation and provenance
```

All review-critical links must resolve inside this repository or its own GitHub release surface.

## Build requirements

### 1. Root storefront

Replace the current cross-release storefront section with a standalone PNP review route.

The root README must declare:

```text
PUBLIC_OPERATING_MODE = STANDALONE_REVIEW_FORTRESS
OTHER_PUBLIC_RELEASE_DEPENDENCY = NONE
CROSS_RELEASE_REVIEW_ROUTING = NONE
```

It must prominently expose the canonical score, manuscript, verification scope, finite replay command, claim firewall, current release, citation, and provenance.

### 2. Review directory

Create or complete `review/` with, at minimum:

- `PNP_REVIEW_PACKET_v1_0.MANIFEST.md`
- `PNP_LEAN_VERIFICATION.RECEIPT.md` or a successor receipt containing the exact verified commit/workflow/checker state
- `PNP_REVIEW_RELEASE_v1_0.PARKED.md`
- `PNP_PUBLIC_FORTRESS_CLOSEOUT_v1_0.md`
- this build contract

Locked mathematical source files are not to be silently rewritten.

### 3. Machine-verification boundary

Canonical PNP verification must be separated from unrelated historical or cross-project CI noise.

Current relevant workflows include:

- `.github/workflows/verify-pnp-package.yml`
- `.github/workflows/verify-tryx-formulas.yml`

The fortress build shall identify which workflow is authoritative for package replay and which workflow is authoritative for Lean/independent checking. If a generic TRYX workflow remains, its role must be explicit and it must not confuse the public PNP claim boundary.

### 4. Verification receipt

The receipt must record, where available:

- exact Git commit checked;
- Lean toolchain version;
- Mathlib / dependency revisions;
- workflow run identifiers;
- comparator / independent checker status;
- `sorry` and project-axiom audit state if applicable;
- benchmark counts and disagreement count;
- negative-control result;
- exact theorem scope;
- explicit statement that no conventional polynomial-time P=NP theorem is asserted.

### 5. Release surface

The GitHub Releases storefront must identify the current standalone PNP review release and contain the same claim firewall as the README and manifest.

A release may be refreshed or succeeded, but historical releases are retained as provenance unless an explicit deletion order is given.

### 6. About metadata

Fortress-facing About text should use `ENIAD`, not stale `ENAID`, and should describe semantic closure / finite verification without claiming conventional P=NP.

Suggested Topics:

`p-vs-np`, `sat`, `3-sat`, `boolean-algebra`, `lean4`, `formal-verification`, `reproducible-research`, `mathic`, `eniad`, `tryx`

### 7. Independence test

The completed fortress passes only if a reviewer can perform the following without opening the Navier-Stokes or Snowman repositories:

```text
LOCATE_SCORE = PASS
LOCATE_MANUSCRIPT = PASS
LOCATE_FORMAL_SCOPE = PASS
LOCATE_VERIFICATION_RECEIPT = PASS
RUN_FINITE_REPLAY = PASS
IDENTIFY_OPEN_RUNTIME_OBLIGATION = PASS
CITE_RELEASE = PASS
TRACE_PROVENANCE = PASS
CROSS_RELEASE_DEPENDENCY = NONE
```

## Claim firewall

The final storefront must preserve this sentence in substance:

> The package proves the stated finite-carrier Boolean semantic result and reproduces the recorded finite benchmark. It does not supply the standard-machine polynomial runtime bound required to establish conventional P=NP.

No release badge, passing CI job, benchmark result, MATHIC score, or Lean theorem may silently promote that boundary.

## Closeout target

The fortress is complete only when the following state can be truthfully issued:

```text
PUBLIC_FORTRESS = TRYX-PNP-MATHIC
PUBLIC_FORTRESS_STATUS = CLOSED
STANDALONE_REVIEW_READY = YES
CANONICAL_REVIEW_TARGET = Problem_No_Problem_MATHIC
LEAN_VERIFICATION_PATH = VERIFIED
FINITE_REPLAY_PATH = VERIFIED
REVIEW_PACKET = READY
PUBLICATION_CHAIN = LOCKED
OTHER_PUBLIC_RELEASE_DEPENDENCY = NONE
CROSS_RELEASE_REVIEW_ROUTING = NONE
CONVENTIONAL_P_EQUALS_NP = NOT_ESTABLISHED
CLAIM_PROMOTION = NONE
PNP_NEXT_PUBLICATION_ACTION = NONE
```

Future mathematical claim changes require a versioned successor. Storefront maintenance may continue without reopening the locked mathematical score.
