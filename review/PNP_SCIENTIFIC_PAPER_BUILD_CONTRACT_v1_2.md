# Problem No Problem Scientific Paper Build Contract v1.2

Date: `2026-09-13`

Status: `ACTIVE_PAPER_BUILD_CONTRACT`

Supersedes: `PNP_SCIENTIFIC_PAPER_BUILD_CONTRACT_v1_1.md`

This contract inherits all v1.1 human-presentation, typography, 48-line vertical-grid, theorem, Lean, reference, claim-firewall, Reviewer’s Bench, render, and Feng Shui rules except where explicitly changed below.

## 1. Principal figure count

The PNP scientific paper now contains **five** principal figures.

```text
PRINCIPAL_FIGURE_COUNT = 5
PRINCIPAL_FIGURE_ZONE = 16 BODY_LINE_EQUIVALENTS
PRINCIPAL_FIGURE_ZONE_FRACTION = 1/3 PAGE
MAX_TEXT_ON_FIGURE_PAGE = 32 BODY_LINE_EQUIVALENTS
FIGURE_PLUS_CAPTION_PLUS_BREATHING_ROOM <= 16 LINE_EQUIVALENTS
```

The one-third-page footprint rule applies independently to all five figures.

## 2. Five-figure architecture

### Figure 1 — Problem / boundary

```text
ROLE = SEMANTIC_CLOSURE_VS_COMPLEXITY_BOUNDARY
PLACEMENT = EARLY_BODY_AFTER_CONVENTIONAL_PROBLEM_FRAMING
PROOF_WEIGHT = EXPLANATORY_ONLY
FOOTPRINT = 1/3 PAGE
```

### Figure 2 — TRYX Local Cycle

Figure 2 reuses the successful VPA audit-cell visual language from **Navier-Stokes: A Nonexplosive Alternative V2**.

```text
ROLE = TRYX_LOCAL_VPA_CYCLE
SOURCE_PRESENTATION = NS_ANEA_VPA_AUDIT_CELL
PLACEMENT = TRYX_HUMAN_EXPLANATION_SECTION
PROOF_WEIGHT = EXPLANATORY_ONLY
FOOTPRINT = 1/3 PAGE
```

The visual must communicate, in ordinary language:

```text
V = Value = current state / carrier
P = Perception = comparison / relation
A = Action = committed operation
V' = next Value = successor state
```

For PNP, the surrounding prose connects the cycle to the local semantic mechanism without falsely relabeling the NS physical address variables as PNP mathematics.

The reused graphic is a presentation lineage reference, not a theorem dependency.

### Figure 3 — PNP Local Closure mechanism

```text
ROLE = M1_TO_M9_LOCAL_CLOSURE_MECHANISM
PLACEMENT = MECHANISM / MATHIC DISCUSSION
PROOF_WEIGHT = EXPLANATORY_ONLY
FOOTPRINT = 1/3 PAGE
```

Figure 3 must preserve the visible distinction:

```text
m4. Hinge Siblings
→ m5. Mirror Closure
→ m6. Existential Fold
→ m7. Current Successor
→ m8. Ordinal Succession
```

### Figure 4 — Finite evidence

```text
ROLE = FINITE_BENCHMARK_AND_INDEPENDENT_ORACLE
PLACEMENT = FINITE_EVIDENCE_SECTION
PROOF_WEIGHT = REPRODUCIBILITY_PRESENTATION_ONLY
FOOTPRINT = 1/3 PAGE
```

Required benchmark scope:

```text
3 variables
→ 26 admissible clauses
→ formulas of size 0–4
→ 17,902 formulas
→ 53,706 folds
→ replay vs independent oracle
→ 0 disagreements
→ negative control detected
```

### Figure 5 — Reviewer’s Bench hero

```text
ROLE = REVIEWERS_BENCH_HERO
PLACEMENT = LATE_BODY / REVIEWER_REWARD_TRANSITION
PROOF_WEIGHT = NONE_INDEPENDENT
FOOTPRINT = 1/3 PAGE
REUSE = HERO_POSTER + SPLASH_PAGE
```

The flagship visual lineage is therefore revised to:

```text
FIGURE_5 MASTER
→ PNP HERO POSTER
→ PNP SPLASH PAGE
```

```text
FIGURE_5_BEFORE_PAPER_LOCK = REQUIRED
HERO_POSTER_AFTER_PAPER_LOCK = REQUIRED
SPLASH_AFTER_HERO_POSTER = REQUIRED
SPLASH_BODY_MUTATION = PROHIBITED
```

All former v1.1 references to Figure 4 as the Reviewer’s Bench hero are superseded by Figure 5.

## 3. Section 4 MATHIC formula-table rule

Section 4 remains a two-column table and must visibly read as a table.

```text
SECTION_4_TABLE_COLUMNS = 2
OUTER_TABLE_BORDER = REQUIRED
CENTER_VERTICAL_DIVIDER = REQUIRED
HORIZONTAL_ROW_LINES = REQUIRED
BORDER_STYLE = THIN_NEUTRAL_RULE
BORDER_VISIBILITY = CLEAR_BUT_NOT_HEAVY
```

Exact row presentation remains:

```text
m1. TYPE-SET-0 Distinction
m2. Current #
m3. VPA Rotation
m4. Hinge Siblings
m5. Mirror Closure
m6. Existential Fold
m7. Current Successor
m8. Ordinal Succession
m9. Local Closure
```

Left column: measure identifier + name on one line.

Right column: large formula presentation followed by the small italic explanatory underscore line, maximum two lines.

The table may not be rendered as floating text with invisible borders.

```text
INVISIBLE_SECTION_4_TABLE_GRID = PROHIBITED
```

## 4. Feng Shui audit additions

Every beta audit now additionally checks:

```text
[ ] exactly five principal figure zones exist
[ ] Figure 2 uses the NS ANEA VPA audit-cell presentation
[ ] Figure 3 is the PNP Local Closure mechanism
[ ] Figure 4 is finite evidence
[ ] Figure 5 is Reviewer’s Bench hero
[ ] Figure 5, not Figure 4, carries poster/splash lineage
[ ] Section 4 outer border is visible
[ ] Section 4 center vertical divider is visible
[ ] Section 4 horizontal row rules are visible
[ ] table rules are thin and neutral, not decorative boxes
```

## 5. Claim firewall

No mathematical status changes in v1.2.

```text
BOOLEAN_FOLD_IDENTITY = VERIFIED
FINITE_CARRIER_EXISTENTIAL_SEMANTICS = LEAN_VERIFIED
SAT_UNSAT_TERMINAL_SEMANTICS = LEAN_VERIFIED
LEAN_MATHIC_SYNCHRONIZATION = BUILD_PASS
FINITE_PACKAGE_REPLAY = VERIFIED
INDEPENDENT_INTERMEDIATE_STATE_CHECK = VERIFIED
NEGATIVE_CONTROL_DETECTION = VERIFIED

REPRESENTATION_SIZE_BOUND = OPEN
SUBSTITUTION_COST_BOUND = OPEN
MULTIPLICATION_OR_EQUIVALENT_COMPOSITION_COST_BOUND = OPEN
NORMALIZATION_COST_BOUND = OPEN
EQUALITY_OR_COMPATIBILITY_COST_BOUND = OPEN
WITNESS_RECONSTRUCTION_COMPLEXITY = OPEN
STANDARD_MACHINE_POLYNOMIAL_RUNTIME_BOUND = OPEN
CONVENTIONAL_P_EQUALS_NP = NOT_ESTABLISHED
CLAIM_PROMOTION = NONE
```

End: `PNP_SCIENTIFIC_PAPER_BUILD_CONTRACT_v1_2`
